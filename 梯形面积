#include <stdio.h>
#include <math.h>
double area(double a,double b,double h) {
  double s;
  s = (a+b)*h/2;
  return s;}
//如解答错误，注意检查错误信息提示，对比预期输出和实际输出之间的差异
int main() {
// your code
  double a,b,h;
  scanf("%lf %lf %lf",&a,&b,&h);
  double s=area(a,b,h);
  printf("%.2f\n",s);
}
